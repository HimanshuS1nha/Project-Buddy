import { Button } from "@/components/ui/button";

const HomePage = () => {
  return (
    <div className="flex items-center justify-center w-full h-full text-red-600 text-5xl font-extrabold">
      <Button>HomePage</Button>
    </div>
  );
};

export default HomePage;
